# class Solution:
#     def groupThePeople(self, groupSizes: List[int]) -> List[List[int]]:
#         return


groupSizes1 = [3,3,3,3,3,1,3]
groupSizes2 = [2,1,3,3,3,2]


answers = []
dict = {}

for i in range(len(groupSizes1)):
    isKey = True

    # dict의 키 여부 체크
    # if (groupSizes1[i] in dict):


    # 없으면 키 : 리스트 생성

    # 키 있고 자리 없으면 answers에 append
    # answers.append()후 키 리스트 초기화


    # 리스트에 ID append
    dict[i] = groupSizes1[i]



# 마지막에 꽉 찬 리스트 체크